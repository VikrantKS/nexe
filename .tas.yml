---
framework: mocha
tier: small
blocklist:
  - "./test/new_test_suite.spec.ts##new suite 1"

postMerge:
  pattern: 
    - "./test/**/*.spec.ts"
   
preMerge:
  pattern:
    - "./test/**/*.spec.ts"
preRun:
     command:
        - npm ci
postRun:
     command:
        - node --version
# parallelism : 4
